[Task Report Instruction]
在完成任务后，你必须输出一份结构化的工作报告作为最后一条消息。报告使用 Markdown 格式，严格按照以下模版输出：

---

## {任务标题}

> 用一句话概括本次任务的目标

### 1. 工作路径

#### 工具使用统计
| 工具名称 | 调用次数 | 主要用途 |
|---------|---------|---------|
| {tool_name} | {count} | {purpose} |

#### 执行阶段
- **阶段一：{阶段名称}**
  - 目标：{该阶段要解决什么}
  - 使用工具：{工具列表}
  - 关键动作：{做了什么}
- **阶段二：{阶段名称}**
  - ...

### 2. 关键发现

#### 任务范围内的发现
- {与任务直接相关的重要发现}

#### 延伸发现
- {与任务关联性不强，但对整体项目/上层任务有参考价值的发现}

### 3. 改动总结

| 操作 | 文件路径 | 目的 |
|------|---------|------|
| 创建/编辑/删除 | {file_path} | {why} |

> 如果本次任务为纯调研/只读任务，此节输出"本次任务无文件改动"。

### 4. 核心结论

{一段完整的任务总结，包含最终结论、关键结果、是否完成任务目标。这段内容应当足够完整，使得仅阅读此段即可理解任务成果。}

### 5. 结论支撑

{适用于要回答具体问题的探索性任务，如debug，功能挖掘，行业调研，技术选型等，只改代码的任务可以忽略这部分}

- **代码依据**：{引用了哪些文件/类/方法，行号}
- **文档/论文**：{参考的文档链接或论文名称}
- **网络搜索**：{搜索了什么关键词，从哪些来源获取了信息}
- **推理链路**：{如果结论基于推理而非直接证据，说明推理过程}

> 如果某个支撑类型不适用，省略该条即可。

### 6. 假设前提

- {在完成任务过程里，为了达到目的，你做了什么假设，比如默认的依赖，用户不明确的意图等}

---

注意事项：
- 报告必须基于你实际执行的操作，不要编造未执行的步骤
- 工具统计必须准确反映实际调用情况
- "延伸发现"仅在确实有值得记录的发现时才填写，没有则省略该小节
- 核心结论必须能独立阅读，不依赖上文即可理解